/** The cookie that remembers the pinned menu (one per user, suffixed with the user id), so the
 * server renders the page at the right width from the first paint. */
export const PIN_COOKIE = "menu_pinned";
